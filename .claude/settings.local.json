{
  "permissions": {
    "allow": [
      "Bash(npm install bcryptjs jsonwebtoken)",
      "Bash(npm install --save-dev @types/bcryptjs @types/jsonwebtoken)",
      "Bash(npm install dotenv)",
      "Bash(node -e \":*)"
    ]
  }
}
